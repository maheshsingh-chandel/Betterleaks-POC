repos:
  - repo: local
    hooks:
      - id: betterleaks
        name: Betterleaks pre-commit secret scan
        entry: betterleaks git . --pre-commit --config betterleaks.toml --redact --confidence low
        language: system
        pass_filenames: false
        stages: [pre-commit]

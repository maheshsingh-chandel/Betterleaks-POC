repos:
  - repo: local
    hooks:
      - id: betterleaks
        name: Betterleaks pre-commit secret scan
        entry: .githooks/pre-commit
        language: system
        pass_filenames: false
        stages: [pre-commit]
